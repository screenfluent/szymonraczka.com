# ADR Format

ADRs live in `docs/adr/` and use sequential numbering: `0001-slug.md`.

```md
# {Short title}

{1-3 sentences: context, decision, why.}
```

Create ADRs only when the decision is hard to reverse, surprising without context, and a real trade-off.
